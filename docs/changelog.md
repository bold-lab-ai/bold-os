# Changelog — How to Submit a Paper

Build history of the files in this folder (`how-to-submit-a-paper.html`, `audit-board.html`, and the supporting docs — `internal-qa-review.html` was part of this suite until it was removed 2026-09-11, folded into `audit-board.html`; entries below that predate the removal still refer to it, left as-is since this is a historical record). This is a record of what was built and when — not a comparison against BOLDiquette. For how the process itself differs from BOLDiquette, and what we want changed there, see **`DIFF.md`**.

## 2026-09-14

### App Home dashboard deployed, debugged live, now fully working

Eduardo deployed `#3`'s Cloud Function himself and we live-tested it together against the real Slack workspace over two sessions. Found and fixed several real production bugs along the way, worst-first: **Socket Mode was enabled** on the Slack app, silently routing all Events API/Interactivity traffic to a WebSocket instead of our Request URL — zero requests ever arrived despite every other setting being correct; took ruling out client caching, app-approval, and a possible duplicate app before finding it, then adding unconditional request-received logging to make the "config looks right, nothing arrives" class of bug diagnosable next time. Once traffic arrived, hit a second real bug: `res.send()` before an un-awaited async handler doesn't reliably finish on Cloud Functions v2 (Cloud Run throttles CPU right after the response) — fixed by awaiting the real work before responding. Then a missed step: `firestore.indexes.json`'s overrides were committed but never actually deployed (`--only functions` doesn't touch Firestore config) — fixed via `firebase deploy --only firestore:indexes`. Then a genuine data-loss bug: the reviewer picker's `static_select` silently truncated at Slack's 100-option cap against the 274-person roster ("stops at letter H") — replaced with `external_select` + a live `block_suggestion` search handler. Also found two pre-existing test cards with pre-migration plain-string `authors`, fixed by hand (roster name-matching, confirmed with Eduardo, no blind guessing) for the unambiguous ones. Finally, added a live `onSnapshot` listener to `audit-board.html`'s board view so a reviewer assigned from Slack shows up without a manual reload. Confirmed end-to-end: dashboard renders real data, search finds people past the old cap, reviewer assignment writes back and fires the existing DM notification, web app updates live. See `docs/FIREBASE.md`'s "Real bugs found live-testing" for the full account.

## 2026-09-12

### Built `#3`: Slack App Home dashboard + reviewer assignment (not yet deployed)

New HTTP-triggered Cloud Function, `exports.slackEvents` (`functions/index.js`, branch `feature/slack-app-home`), handling both Slack's Events API (`app_home_opened`, plus the `url_verification` handshake) and Interactivity (`block_actions`) at one URL, with Slack request-signature verification (a new `SLACK_SIGNING_SECRET` secret) gating everything else. Opening the Home tab renders four sections — Registered, Reviewing (Junior/Senior), Authoring — from four parallel `collectionGroup('cards')` queries, capped to the 10 most-recently-updated each; each Registered card carries an inline reviewer-picker that writes `reviewers.{role}` after re-checking the same submitter-or-PI/admin permission `firestore.rules` already enforces client-side (this write goes through the Admin SDK, which bypasses Security Rules entirely, so the check has to be reimplemented server-side). Client-side prerequisite: a denormalized `authorEmails: string[]` field alongside `authors`, since Firestore's `array-contains` can't match inside `{name,email}` objects — wired into `onSaveEdit`/`onAddCard`/`normalizeCard`. New `firestore.indexes.json` field overrides for the four query fields, explicitly flagged as unverified against a live deploy. 40 standalone unit-test cases pass; nothing has been deployed or tested against a live Slack workspace yet — still needed: the signing secret, a deploy, Slack app config (App Home / Event Subscriptions / Interactivity & Shortcuts), an `authorEmails` backfill on pre-existing cards, and a live smoke test. See `docs/FIREBASE.md`'s "Slack App Home dashboard + reviewer assignment" section.

### Expanded `#3`: a personal papers dashboard in Slack's App Home, reviewer assignment folded in

Eduardo asked about a Slack App Home tab showing, per person, the papers they've registered, are reviewing, or are an author on — with reviewer assignment (already scoped in `ml-conference-cycle#3`) available inline for whoever owns the paper. Combined into `#3` rather than filing separately, since the reviewer-picker is genuinely one interactive element inside this dashboard, not a second feature. Renamed the issue and added the dashboard's own requirements: App Home + `app_home_opened` Event Subscriptions (same Request URL infra `#3` already needed), a new Firestore collection-group query across every venue's cards (nothing today queries across boards — likely needs a denormalized `authorEmails: string[]` alongside `authors`, since Firestore's `array-contains` can't partially match the `{name,email}` objects), and rendering the dashboard itself as Block Kit. Not built.

### Dropped the notification button; reviewer outcomes get their own ✅/❌

Two more rounds of feedback on the message content, in between the previous entry and the comment-routing one below:

- **The separate button is gone.** Once the title itself became a real clickable link (previous entry), a button pointing at the same place was a redundant second link in every message — dropped it and reverted from Slack Block Kit back to plain `text` (Slack's default mrkdwn rendering already shows the bold link correctly with no `blocks` needed).
- **Reviewer sign-off outcomes now get their own emoji** — ✅ for Approved, ❌ for Changes requested, ↩️ for reverting back to In review (e.g. unticking a checklist box after being approved) — instead of a single 🔍 covering the whole message regardless of outcome. A mixed outcome (one role approves, the other requests changes, in the same write) now shows both glyphs distinctly. 🔍 was freed up and reassigned to "you've been handed a reviewing task" (previously 📋).
- **Emoji now lead every message, including these** — the outcome emoji originally landed after the linked title (a leftover from when they were inline per role); moved to the front so every message type has the same emoji-then-title shape.

Extended the test suite to 22 cases, redeployed.

### Notification comment routing: only who actually needs to answer

More feedback after seeing real messages: comments were over-notifying — everyone with a stake in the card got pinged for every comment, regardless of whether it was actually relevant to them. Redesigned per Eduardo:

- **Discussion, a new top-level message** — unchanged: every stakeholder (submitter + both reviewers) except the poster.
- **Discussion, a reply** — now only the people already in *that* thread (the original poster + anyone who'd already replied), not the full stakeholder set. Replying to one side conversation no longer loops in someone who was never part of it.
- **Checklist-item comment, submitter posts** — both reviewers (a checklist item has one shared thread, not separate junior/senior ones, so there's no way to tell which reviewer it's meant for).
- **Checklist-item comment, either reviewer posts** — just the submitter, not the other reviewer (the two review passes are independent of each other).

Also fixed: the reviewer-sign-off emoji (✅/❌ from the previous entry) now lead the message, same position as every other event's icon — they'd landed after the linked title, inconsistent with the rest.

Extended the test suite to 36 cases (14 new, covering every comment-routing branch), redeployed.

### Clearer emoji, and the title is now a clickable link too

More feedback on the actual message content, redeployed: the emoji weren't clear, and the link should be on the title, not just the button.

- **`linkedTitle(text, url)`** produces Slack's bold-link mrkdwn (`<url|*text*>`) — every headline's paper/venue name now uses this instead of a bare `*title*`, so the title itself is clickable, in addition to the existing button (not instead of it).
- **Emoji regrouped into two deliberate families.** 🔔 now marks anything that's actually asking the recipient to decide something — a venue proposal awaiting approval, a card reaching the PI-approval step — both of which used to have unrelated icons (📄, 🖊️) with no shared meaning. Everything else is a plain FYI icon: 🔍 (a reviewer's sign-off changed on your card), 📋 (a new reviewing assignment — was 👀, replaced since it read more like "watch this" than "here's a task"), ➡️ and 💬 unchanged (already clear).

Extended the test suite to 20 cases, redeployed.

### Filed: a lab-wide Projects Board

Filed [`ml-conference-cycle#6`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/6) — a page where anyone can see every active BOLD research project (abstract, research questions, findings, publications, leader, collaborators, experiments, a dedicated Slack channel), with a "Collaborate" button that adds the clicker as a collaborator and auto-invites them to that project's Slack channel. Notably scoped **against this repo** rather than a new one — per Eduardo, `ml-conference-cycle` is expected to grow into the lab's broader Operating System over time, and no dedicated repo exists yet for that still-unplanned, separate lab discussion. The auto-invite half needs a Cloud Function (same "Bot token can't live client-side" constraint as `#1`) and the `conversations:write.invites` scope — a channel invite for an existing workspace member, the simpler case already distinguished from the plan-tier-gated *workspace* auto-invite idea noted once before in this doc's old Phasing section.

### Real deep links + Slack Block Kit for notification messages

Feedback after the first round of live-Slack testing: the messages were plain text, and the link in every one of them just went to the app's front door, not the specific paper or venue. Fixed both, redeployed:

- **`audit-board.html` now has real, bookmarkable URLs.** `history.pushState()` never set the actual browser URL before this — Back/Forward worked, but a link *into* the app could only ever land on the venues list. `stateUrl()` builds a hash (`#board=<id>` or `#board=<id>&card=<id>` — a hash, not a path, since this is a static GitHub Pages site with no server-side routing) and `pushNavState()` now sets it as the real URL. `parseDeepLinkHash()`/`applyPendingDeepLink()` restore straight to that venue/card on a fresh page load — a missing venue and a venue that exists but isn't visible to this viewer get the identical toast, on purpose.
- **`functions/index.js` sends real Slack Block Kit messages** (`buildMessage()` — a headline section plus a button linking to the actual venue/card, via `venueUrl()`/`cardUrl()` mirroring the client's own hash scheme) instead of a flat text blob with a pasted generic URL.

Extended the standalone test suite to 18 cases (deep-link URL building, round-tripping, and the Block Kit message shape) before redeploying. Redeployed cleanly (an update this time, not a first create, so no Eventarc propagation delay to wait out).

### Slack notifications deployed and verified live against real Slack

`onVenueProposed`/`onCardWritten` deployed to `bold-d7ff2` (`europe-west2`, Node 20, 2nd gen). First deploy attempt failed with a well-known, expected first-time-Gen2 hiccup (`Permission denied while using the Eventarc Service Agent` — the service agent's IAM permissions hadn't propagated yet); retried a few minutes later and both functions created successfully. Also ran `firebase functions:artifacts:setpolicy` (a 1-day container-image cleanup policy) — the deploy otherwise leaves old container images accumulating indefinitely in Artifact Registry.

Verified live: Eduardo created a real test venue and paper against production (the local branch checkout already points at real Firestore), assigned himself as reviewer, and confirmed real Slack DMs landed — "many tests, all working well." `feature/slack-notifications` still isn't merged to `main` (nothing in the client changed as part of deploying the functions — this was purely `functions/` + `firebase.json`), so this is a case of server-side infra being live before the branch merges; the branch itself only needs merging for the `docs/`/`.gitignore`/`firebase.json` changes to land on `main`'s history, since the actual Cloud Functions are already deployed regardless of which branch HEAD points to (Cloud Functions deploys aren't tied to a git branch the way GitHub Pages' branch-deploy is).

### Filed two follow-ups discussed while scoping Slack notifications

While confirming which Slack Bot Token Scopes to add for `ml-conference-cycle#1`'s reinstall, two more ideas came up that are real but out of scope for this pass — filed rather than built, same as `#2` (venue transfer) earlier:
- [`ml-conference-cycle#3`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/3) — assigning reviewers from Slack itself (a slash command and/or a button on the notification DMs). Confirmed with Eduardo: a **structured** action (`commands` scope, a new HTTP-triggered Cloud Function, Slack's Interactivity config), explicitly not a conversational AI-assistant experience (which would need Slack's separate Agents & Assistants platform, `assistant:write` — considered and ruled out for now).
- [`ml-conference-cycle#4`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/4) — a persistent in-Slack notification history via Canvas (`canvases:read`/`canvases:write`), Eduardo's idea from the same conversation.

Both scopes (`commands`, `canvases:read`/`write`) are being added in the same Slack app reinstall as `chat:write`/`im:write` so a third reinstall isn't needed later — granted ahead of either feature actually being built.

Also filed [`ml-conference-cycle#5`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/5) — a conversational Slack assistant (Slack's Agents & Assistants platform, `assistant:write`) where each person talks to *their own* Claude/ChatGPT account rather than one shared backend LLM call. Explicitly not decided to build (Eduardo: "for now we see what happens later") — filed purely to keep the idea findable, and to record that `assistant:write` alone doesn't provide this: it's just the assistant UI framework, with no built-in way to hand a conversation off to a user's own personal AI login. Making that real would need a from-scratch bring-your-own-key system (per-user API key storage, same discipline as the Slack Bot token). No scope requested for this one — nothing granted, nothing built.

### Slack DM notifications — built, on a branch, not yet deployed

Picked up `ml-conference-cycle#1`, on `feature/slack-notifications` (after the identity fixes below, which this depends on). `functions/index.js` is the project's first Cloud Function — the first server-side code of any kind — since sending a Slack DM needs a Bot token, a real credential that can never live in `audit-board.html`'s client-side source.

Two Firestore-triggered functions cover all four notification triggers Eduardo picked: a venue proposal needing approval (DMs every PI/admin), a reviewer's sign-off changing on your card or both reviewers approving and auto-advancing it (DMs the submitter), being newly assigned as a reviewer (DMs that person), any status change (DMs the submitter), reaching the PI-approval step specifically (also DMs the PI), and a new Discussion message or checklist comment (DMs submitter + both reviewers, excluding the poster).

Structure follows the same pattern established throughout this session's client-side work: `cardEventsToNotify(before, after, title)` is pure decision logic — no Firestore, no Slack, no `await` — that computes what to notify from a before/after diff; the actual Firestore trigger is a thin wrapper that resolves each event's emails to Slack ids (via the already-synced `people` roster) and sends. Verified the whole decision surface with 17 standalone unit-test cases (every trigger, several "one write implies two notifications" cases, the "no email on file" no-op case) before ever touching a real Slack workspace.

Not deployed yet — needs `chat:write`/`im:write` Slack Bot Token Scopes added and the app reinstalled, then `firebase functions:secrets:set SLACK_BOT_TOKEN` and `firebase deploy --only functions`. Also discussed, not built: `canvases:read`/`canvases:write` scopes for a persistent notification-history log (Eduardo's idea) — worth adding in the same Slack reinstall since it avoids a third round-trip, but the canvas-logging feature itself is a separate follow-up once DM-sending is confirmed working.

### Real identity for comments and authors — prerequisite for Slack notifications, on a branch

Started work on the open Slack-notifications issue (`ml-conference-cycle#1`, branch `feature/slack-notifications`). Before writing any Cloud Function code, hit a real data-model gap: two of the requested notification triggers ("reaches PI-approval," "someone comments") had no reliable email to send to — the PI is just a free-text name (the last entry in `authors`), and comment/discussion authorship was a free-text field with only a suggestion list, no roster binding at all.

Per Eduardo, fixed both at the source rather than working around them:

1. **Comments and Discussion messages now use the signed-in poster's real identity** — `author`/`authorEmail` come from `state.currentUser`, same as `submittedBy`/`reviewers`/`proposedBy` already do, instead of a free-text name typed into the form. Posting requires being signed in (`requireSignedIn()`). The four "Your name" input fields are gone from the Discussion/checklist-feedback forms. Older messages posted before this change keep their old free-text `author` and simply have no `authorEmail`.
2. **Authors are no longer free text either.** Each row is now a roster `<select>` (same shape as the reviewer picker), plus a "+ New author (not in the workspace)…" option that swaps the row to two typed inputs (Name, Email) for a genuine external co-author — that typed entry is deliberately *not* added to the shared `people` roster, only stored on that card. `card.authors` is now `[{name, email}]`, not `[name]`; the PI (still just the last entry) now has a real email. `authorRowsIssue()` validates both modals: the PI row must be complete, and no other non-blank row can be half-filled.

`normalizeCard()` migrates any card still holding plain-string authors to `{name, email: ''}` — email is unrecoverable for old data, so a PI-approval notification on an old, not-yet-re-saved card will do nothing until someone re-saves its author list through the new picker. No Security Rules changes needed — nothing in `firestore.rules` reads `authors`. Verified the validation/migration/search logic (`authorsForSave`, `authorRowsIssue`, `cardPi`, the `normalizeCard` authors migration, and `matchesTextQuery`'s author search) with standalone unit tests (9 + 5 + 3 cases) before moving on to the actual Cloud Function.

### "+ Register paper" no longer shows at all on a pending venue

Per Eduardo, it shouldn't just be unusable, it shouldn't be there — a pending venue's page (`feature/venue-proposals`, still unshipped) previously showed a disabled "+ Register paper" with a tooltip; now `renderBoard()` omits the button entirely when the venue's `status` is `'pending'`. `openModal()`'s existing "this venue isn't approved yet" check stays in place as a defense-in-depth backstop even though there's no button left to trigger it through normally.

### Approve/Reject also inline in the list, green/red styling, "+ Propose venue" wording, notification triggers noted on the tracking issue

Three more revisions to venue proposals (still `feature/venue-proposals`, unshipped) plus a notifications note, all per Eduardo:

1. **Approve/Reject moved back to being available in the list too, not only after clicking in.** Both now live in the "Pending your approval" list row *and* the venue detail page — same `onApproveVenue`/`onRejectVenue` either way. Since a row with action buttons in it can't be the whole-row `<button>` the rest of the list uses, that row is now a `<div>` with its own small nested clickable name (`.venue-name-btn`) plus the two actions as siblings.
2. **Approve/Reject now match — same size and weight, just green vs. red** (`.btn-approve`/`.btn-reject`). Previously Approve was a primary blue button and Reject a plain text link, which visually implied one was the "default" answer; a symmetric either-way pairing reads better for a genuine yes/no decision.
3. **"+ New venue" now reads "+ Propose venue"** for anyone who isn't a confirmed PI/admin (`updateNewVenueButtonState()`, driven by `state.canApproveVenues`) — the modal's own title and submit label follow suit ("Propose new venue"/"Propose" vs. "New venue"/"Create"), same form and `onCreateBoard()` either way.

Also: added two notification triggers to the open Cloud-Functions-notifications plan (`ml-conference-cycle#1`, not this venue-proposals work) — [commented on the issue](https://github.com/bold-lab-ai/ml-conference-cycle/issues/1#issuecomment-5638260904) rather than building anything, since that whole feature is still unbuilt. PIs/admins should be notified when a venue proposal needs approval/rejection; more generally, per Eduardo, the app should be scouted for any case where someone else's own action is something *you* need to know about or act on but nothing currently tells you (the other concrete instance flagged: a reviewer setting their sign-off on your card, or both reviewers approving and auto-advancing it) — framed as an ongoing thing to watch for as the app grows, not a fixed list.

### Filed: transferring a paper to a different venue

Filed [`ml-conference-cycle#2`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/2) rather than building it blind — moving a card between venues is a cross-subcollection move (`boards/{boardId}/cards/{cardId}` to a different `boardId`), not a field update, and needs to be atomic or safely recoverable if it fails partway. Per Eduardo, restricted to the paper's owner or a PI — deliberately *not* admins, the first place in the app that excludes admins from something PIs can do; flagged in the issue to confirm that's intentional before building. Open questions the issue tracks: whether status/checklist/reviewers reset on transfer or carry over, and whether a `history` entry records the move.

### Venue proposals, revised while testing: full detail view, real proposer identity, a Reject button — still on a branch

Testing the venue-proposals feature (below) surfaced two real gaps, both fixed on the same branch, still unshipped:

1. **Pending venues weren't clickable at all.** Fixed: every row — approved or pending — now opens the normal venue page. A pending one shows a "Pending approval" badge, an empty board, "+ Register paper" disabled, and (new) an Approve/Reject pair in the header next to Edit venue, shown only to a confirmed PI/admin. Reviewing a proposal is now a click-in, not a blind list action — the Approve button that used to sit directly in the list row is gone.
2. **"Proposed by [name]" alone wasn't enough to actually identify who proposed something** — a name can be generic or ambiguous with more than one pending proposal to review. `proposerLabel()` now always renders name **and** email together, used consistently in both the list row and the venue detail page.

Also added: a **Reject** button, alongside Approve, for a pending venue a PI/admin doesn't want. `onRejectVenue()` is the same underlying delete `onDeleteBoard()` already does (nothing worth keeping on an unapproved venue), but with its own confirm dialog and toast wording, and kept as a visually distinct button from "Delete this venue" — which now only appears once a venue is actually approved, since deleting a live venue with real papers on it is a much bigger action than declining a proposal.

### Big red warning banner when FIRESTORE_USE_EMULATOR is true — built on a branch

Per Eduardo, a safety net for the exact mistake that shipped once already today (`FIRESTORE_USE_EMULATOR = true` committed and deployed, silently breaking the live site for anyone without a local emulator running). Added `#emulatorWarning`/`.emulator-warning` — a loud, sticky red bar across the top of the page, shown whenever that flag is `true`. Toggled once, right next to the flag's own declaration, not tied to any render function since the flag never changes after page load. Built alongside venue proposals (below) on `feature/venue-proposals`, per the new branch-first workflow — not merged/deployed yet.

### Venue proposals — built on a branch, not yet shipped

The site's live and in real use now, so from here on new work happens on a feature branch (`feature/venue-proposals` for this one) before merging to `main` and deploying — this entry, and everything in it, is **not live yet**.

Per Eduardo: any lab member should be able to propose a new venue — same form as an admin creating one — but the proposal is only visible to the proposer and PI/admin until a PI/admin approves it. Implemented as a `status` field on `boards/{boardId}` (`'approved'` or `'pending'`, missing means approved) plus `proposedBy` on pending ones. `onCreateBoard()` always tries a direct create first, exactly like today, and only falls back to a pending, self-attributed proposal on the specific failure that means "you're not PI/admin" — there's no reliable way for the client to know that ahead of time (PI/admin status is permanently unreadable client-side, same constraint as Rush mode's own permission check). The venues list splits into approved (normal, openable), "Your proposals awaiting approval" (own pending, informational only), and — for a confirmed PI/admin — "Pending your approval" with an Approve button per row.

The real access control is in `firestore.rules`, not just the UI: a pending venue's `read` now requires being the proposer or a PI/admin. The tricky part was that Firestore rejects an entire list query outright if *any* candidate document could fail the rule for the requester — it doesn't silently filter results — so the old plain `boards.get()` had to become three separate, purpose-built queries (`fetchVisibleBoards()`): approved venues (safe for everyone), the caller's own pending proposals (safe for everyone, ownership-filtered), and all pending proposals unfiltered (succeeds only for a PI/admin — this is also how the client learns whether to show the approval section at all, there being no other way to ask).

**Known follow-up before this can actually ship:** every venue created before today has no `status` field, and the new "approved" query can't match a field that isn't there (unlike a rule's in-line default, which does cover single-document reads). Needs a one-time backfill of every existing venue to `status:'approved'`, done under the *old* rules, before the new rules and query-based loading deploy together. Full design, including that sequencing, is in `docs/FIREBASE.md`'s "Venue proposals" section. Verified the rule logic (a 12-case truth table covering read/create across owner/PI-admin/unrelated-reviewer) and the query-merge/section-split logic (an 11-case test) standalone — no live Firestore or browser test yet, since this hasn't deployed.

### Reviewer reassignment restricted to the submitter and PI/admin

Eduardo asked to confirm who can assign reviewers; checking the actual Security Rules turned up a real gap — the per-card `update` rule was whole-document, so an already-assigned reviewer could also reassign reviewers (including moving themselves off a card, or someone else onto one), not just the submitter/PI/admin the question assumed. Fixed in `firestore.rules`: the `reviewers` field specifically may now only change if the writer is the submitter or a PI/admin, compared by map equality against the stored value; the existing base condition (submitter/junior/senior/PI-admin) still governs every other field, so an assigned reviewer keeps full access to their own card's checklist, review state, discussion, and status. Deployed and verified live via `firebase_get_security_rules` matching the file exactly. Sanity-checked the new access matrix (owner/PI-admin/assigned-reviewer × reassigning-reviewers/editing-other-fields) with a 7-case standalone truth-table test before deploying, since there's no local Firestore-rules unit-test harness in this repo.

**Deliberately not mirrored client-side beyond a hint.** `reviewerInputHtml()`'s picker stays enabled for every signed-in lab member — PI/admin status isn't readable by the client (`roles/{roleId}` stays deny-all by design), so greying the picker out for "not the owner" would also incorrectly block a real PI/admin, who has no other way to prove it to the client. Added a `title` tooltip for a non-owner instead of `disabled`; an unauthorized save still fails cleanly with the existing permission-denied toast. Same trade-off already accepted for Rush mode's own permission check.

### Checklist ticking restricted to the assigned reviewer; ticking now drives their own Approved status automatically

Per Eduardo: "only the assigned respective reviewer should be able to tick boxes in the project page. The status should turn to 'Approved' automatically if all checks have been ticked, and back to 'In Review' if any is unticked but the card is in 'Approved' status." Two changes to the checklist, both in `onToggleChecklist()`/`checklistTabHtml()`:

1. **Access restricted.** A role's checkboxes are only enabled for the reviewer actually assigned to it (`card.reviewers.junior`/`.senior` matching the signed-in email) — everyone else sees them disabled, with a tooltip. Same client-side-only shape as the tri-state Approved/Changes requested/In review buttons added earlier today; the Security Rules' card-update condition stays whole-document, not field-level.

2. **Auto-sync.** Every tick recomputes that role's checklist completeness and syncs `jrReviewState`/`srReviewState` to match: all boxes ticked → `'approved'`; any unticked while currently `'approved'` → back to `'in_review'`. Deliberately **never touches an explicit `'changes_requested'`** either way — that's a stronger, separate signal a reviewer set on purpose, not something checklist state should silently clear or overwrite. This can cascade into the existing both-approved auto-advance-to-Reviewed behavior (added earlier today) when the box that completes the checklist is also the one that completes both reviewers' sign-off.

Because a tick can now change more than the checklist itself, `onToggleChecklist()`'s existing partial refresh (gate banner + move buttons only, to preserve scroll position) is now conditional — it falls back to a full `renderCardDetail()` on the specific tick that crosses a completion boundary (changes a reviewer's state, or auto-advances the card), and keeps the light partial refresh for every ordinary tick that doesn't. Verified the access map and the full decision matrix (complete → approved, uncomplete while approved → in_review, changes_requested preserved both directions, cascading into auto-advance vs. not) with an 18-case standalone unit test.

### Advance from Drafted to Reviewed is now also gated on both reviewers approving

Per Eduardo, immediately after the redesign below: manually advancing a card from Drafted to Reviewed (the Advance button, the status dropdown, or reverting back in and moving forward again) should be blocked — greyed out with a reason, same as the existing checklist gate — until both reviewers have actually approved, not just auto-advance covering the happy path. `advanceBlockReason()` and `onChangeStatus()` both now also check `reviewFilterState(card) === 'approved'` at the same `crossesGate()` boundary the checklist-completion check already uses, independent of it — a card needs both the checklist finished *and* both reviewers' explicit approval, checked separately, to leave Drafted. `onSetReviewState`'s auto-advance already only fired once this same condition held, so it needed no change. Verified with a 6-case unit test covering each gate firing alone and together.

### Review sign-off is now per-reviewer, three buttons each; approving both auto-advances the card

Redesigned the review-status controls added earlier today (below), per Eduardo's follow-up: "we should really have three buttons (same line) under each reviewer, where only that reviewer can click to say 'In review' 'Changes requested' 'Approved'." Replaced the shared `changesRequested` flag + independent `jrApproved`/`srApproved` booleans with two per-reviewer tri-state fields, `jrReviewState`/`srReviewState`, each one of `in_review` / `changes_requested` / `approved`. Rendered as a row of three buttons directly under each reviewer's picker in `.card-reviewers` (`reviewStateButtonsHtml()`), on both the card face and the detail page — one row for Jr, one for Sr, both always visible while the card's in Drafted.

Click access is restricted to the actual assigned reviewer: a row is only enabled when the signed-in user's email matches `card.reviewers.junior`/`.senior` for that row; everyone else sees it, disabled, with a tooltip explaining why. This is client-side only — the Security Rules' card-update condition already requires the writer's email to match the submitter, a reviewer, or a PI/admin, but at the whole-document level, not per-field; a determined bypass of the UI could still write the other reviewer's field. Making that a real field-level rule is a bigger, separate piece of work, not attempted here.

New: once both reviewers independently reach "Approved" on a card still in Drafted, it now auto-advances straight to Reviewed (`final_draft`) — no separate manual step. Still held to the same checklist-completion gate as any other move that crosses `GATE_STATUS`; if the checklist isn't finished yet, the status is left alone (it advances next time a state flips once the checklist catches up, or someone Advances by hand).

`reviewFilterState`/`reviewBadges` and the board search bar's review-status filter (unchanged from the version below) keep working — `changes_requested` still wins the overall bucket if either reviewer is in that state; `approved` only once both are. `normalizeCard()` migrates any card still holding the earlier shape so no reviewer's prior sign-off is lost. Verified the tri-state logic, the migration, and the auto-advance gate (including "checklist incomplete → doesn't advance") with a standalone unit test, same approach as everything else today — still no connected browser this session.

### Review status is no longer automatic; added a filter for it

"Approved" used to be derived automatically from `checklistComplete()` — every box ticked by both reviewers. Per Eduardo: ticking every box isn't the same as a reviewer actually signing off, and it shouldn't happen without a manual action. Split into two new independent, manually-toggled flags — `jrApproved` and `srApproved` (one-click buttons on the card face and detail page, same shape as the existing `changesRequested` toggle) — neither derived from the checklist at all. `changesRequested` was already manual; unchanged.

`reviewFilterState(card)` gives the three buckets for a card's colour and the new filter: `changes_requested` (wins if set) → `approved` (only once **both** jr and sr have signed off) → `in_review` (the default, including a partial sign-off from just one reviewer). `reviewBadges(card)` shows the fuller picture on the card itself — a single "Approved" badge once both are true, or "JR approved"/"SR approved" individually if only one is, so partial progress is visible without counting as done.

The checklist-completion gate on actually advancing a card past Reviewed (`crossesGate`/`GATE_STATUS`) is unrelated and unchanged — this sits on top of it, not a replacement, per Eduardo's call when scoping this.

Also added: a second dropdown next to the board's search bar — "Any review status" / "In review" / "Changes requested" / "Approved" — filters the board to one bucket, combined (AND) with the existing text search. Verified the whole review-state model (buckets, badges, the "checklist alone doesn't mean approved" behavior specifically) with a 9-case standalone unit test, same approach as Rush mode below — no connected browser this session.

### Rush mode — a per-venue 4-column board for an imminent deadline

New per-venue toggle, `board.rushMode` — a one-click "Rush mode: Off/On" button in the venue header (not a field in the Edit-venue form; this is "move fast right now," not a considered edit). When on, a venue's board collapses to 4 columns — Registered, Drafted, Reviewed, Submitted — skipping Pitch Day, PI Approval, and the three post-submission columns.

Not a new status vocabulary: `RUSH_STATUS_ORDER` is a coherent subsequence of the existing `STATUS_ORDER` keys (`register`/`draft_review`/`final_draft`/`submit`), so the checklist gate and `onChangeStatus` needed zero changes — full-order indices already answer "did this cross `GATE_STATUS`" correctly regardless of which order a move happened in. `effectiveStatusOrder(board)` picks the active order; `displayStatus(card, board)` projects a card whose true status isn't in the active order onto the last one it's actually passed (e.g. Pitched → shows under Registered) — **without ever rewriting `card.status`**, so toggling is purely a display change, fully reversible, no data loss. A card's status only changes when someone actually moves it, and only to a value from whichever order was active for that move. `.columns`' `min-width` (previously a hardcoded 9-column value) is now computed from the actual rendered column count.

No Firestore Security Rules changes — `rushMode` is just another field on a document create/update already covered by `hasFullWrite()`.

Verified with a standalone unit test against the extracted logic (21 cases: order selection, floor-projection for all 5 omitted statuses, Advance/Revert at both ends of the rush order, dropdown filtering, first-move-after-toggle behavior) — no connected browser available this session for a full click-through, so this is the evidence in place of one.

### Browser Back now navigates within the board, not straight out of it

`audit-board.html` is one page load — list → venue → card was always just an in-memory `state.view` flag, never touching browser history, so pressing Back from a card page skipped over all of that and left the app entirely (whatever was open before `audit-board.html`). Added the standard fix: `history.pushState()` on every navigation (`openVenue`, `openCard`, and the in-app "back" links `backToVenuesList`/`backToBoard` too — treating a step back as new navigation, not `history.back()`, so it works the same whether reached fresh or via Back, and so the browser's Forward button correctly returns to the card/venue you left via an in-app link), and a `popstate` listener that restores the right view (reusing the same render path `openVenue` already uses) when Back/Forward fires. Not wired into `onRemoveCard`'s auto-return-to-board-on-delete — that's a side effect of an action, not navigation. Not verified in a live browser this session (no connected browser tool available); worth a real click-through check once deployed.

### Fixed: moving a card reset the board's horizontal scroll to the start

`.board-scroll` (the kanban columns, wider than most viewports) got rebuilt from scratch on every `renderBoard()` — a fresh `innerHTML`, not a patch — which meant a full scroll-position reset on every single card move, checklist tick, or anything else that re-renders the board. Fixed: `renderBoard()` now reads the old `.board-scroll`'s `scrollLeft` before replacing the HTML, and restores it onto the new one afterward. No more scrolling back to where you were after every action.

### Removed the "Advance" confirmation popup

`window.confirm()` before a card advances (a per-status attestation checklist — "Before moving to Draft & Review, confirm: ...") was causing real problems at Eduardo's end: native browser confirm dialogs sometimes don't appear reliably, and cards were getting stuck waiting on one. Removed the whole mechanism (`STATUS_ADVANCE_CHECKLIST`, `advanceConfirmMessage()`, the `window.confirm()` call) — advancing a card now just happens, no popup. The two *hard* gates (checklist completion before the gate status, reviewers assigned before Draft & Review) are unrelated and untouched — those are enforced checks with a toast message, not confirmation dialogs, and stay. Also untouched: the delete-venue and remove-card confirms, which guard actually-irreversible actions, not a routine status move.

### Zip bundle now builds and commits itself

Added `.github/workflows/zip.yml` — rebuilds `dist/bold-paper-submission-guide.zip` and commits it on every push to `main`, same pattern as `epignatelli/epignatelli.github.io`'s `cv.yml` (build on push, commit back if changed, bot identity). `paths-ignore` on the zip's own path stops the bot's commit from re-triggering itself, which that reference workflow didn't need (it commits into a *different* repo than the one it runs in; this one commits into itself). No more manual `zip -qj ...` + commit step after every doc/page change — removed from `docs/AGENTS.md`'s "After any change" list.

### Permission-denied errors now say so, instead of a misleading "try again"

A non-PI/admin lab member tried to create a venue and got "Could not save the venue list — try again." — correct behavior (venue create/delete requires `hasFullWrite()`), but a misleading message: Security Rules rejections aren't transient, retrying does nothing. `saveBoard()`/`saveBoardsIndex()` now record the Firestore error's `code` (`lastWriteErrorCode`) on failure; a new `saveErrorMessage(fallback)` helper returns "You don't have permission to do that — check with a PI or lab admin." specifically when the code was `permission-denied`, and the original fallback text otherwise. Applied at all 9 places a save failure shows a toast.

### Fixed: "also as plain DIFF.md" showed raw markdown instead of rendering

`diff.html`'s own "also as plain DIFF.md" link pointed at `docs/DIFF.md` directly — GitHub Pages serves `.md` files as raw unstyled text (no Jekyll/build step in this project), so clicking it showed the raw markdown source instead of anything readable. Pointed it at the GitHub repo's own file viewer instead (`.../blob/main/docs/DIFF.md`), which actually renders markdown. Checked every other page for the same mistake — this was the only raw `.md` link anywhere in the served pages.

### Found and fixed: the live site was still pointing Firestore at a local test emulator

A second real person signed in via Slack on the live site and saw "No venues yet" — sign-in worked, but board/card reads were silently failing. Root cause: `FIRESTORE_USE_EMULATOR` was still `true` in the committed file, so the deployed page was trying to reach `localhost:8080` — which doesn't exist on anyone's machine but a local dev session with the emulator running. `AUTH_USE_EMULATOR` was flipped to `false` (real Auth) back when Sign-in-with-Slack was first wired up and tested; `FIRESTORE_USE_EMULATOR` never was, since all local testing this whole session went through the emulator on purpose (see the many earlier entries about seeding it, testing against it, etc.) and nobody had actually driven the deployed page as a second real user until now — my own testing was all via `localhost:8137`, which never exercises what the *deployed* config actually does.

Flipped `FIRESTORE_USE_EMULATOR = false`. Production Firestore itself was already correct and ready — Security Rules deployed and verified live, `roles`/`people` seeded with real data — the client just wasn't pointed at it. Both flags stay available to flip to `true` for local dev; just never commit them that way, per the new comment on that block.

### Pages deploy: tried GitHub Actions, reverted to "Deploy from a branch"

Briefly switched Pages from the default branch-based source to a GitHub Actions workflow (`.github/workflows/pages.yml`: checkout → `configure-pages` → `upload-pages-artifact` → `deploy-pages`, no build step). Reverted the same day, back to the plain branch deploy (`main`, `/`) — removed the workflow file, switched `build_type` back to `legacy` via the API. Same live result either way; going with the simpler default.

### Sign-in prompt is now a real link; homepage hero CTA folded into the text

`audit-board.html`'s signed-out message ("Sign in with Slack (top right) to view...") now has "Sign in" as an actual clickable trigger for the sign-in popup, not just a pointer to go find the masthead button — extracted the popup-flow logic (`triggerSlackSignIn()`) so both the masthead button and this inline one call the same thing. Homepage: removed the separate "Open the Internal Review Board" CTA button, made "Internal Review Board" bold within the existing hero sentence instead (still a link) — one CTA, not two saying almost the same thing. Dropped the now-unused `.hero-cta` CSS.

### `internal-qa-review.html` removed; `how-to-submit-a-paper.html` is the homepage

Removed `internal-qa-review.html` (Internal Review — the standalone simulated-peer-review page) at Eduardo's request: redundant with the checklist already embedded on every board card, which is the one that actually gates a card reaching Approved. Updated every cross-reference: `how-to-submit-a-paper.html`'s Phase 4 link, `audit-board.html`'s own lede, `diff.html` and `docs/DIFF.md` (tool list, the checklist-location line — both hand-kept in sync per `docs/AGENTS.md`'s own rule), `docs/AGENTS.md` (file table, guideline 7 rewritten — there's only one checklist copy now, nothing to "keep in sync"), `docs/TODO.md`, `README.md`. Left `docs/changelog.md`'s own historical entries untouched — they correctly describe a page that existed at the time.

`index.html` reverted to a bare redirect to `how-to-submit-a-paper.html` — that page is the actual homepage now, not a separate landing page listing tools (undoes the multi-link homepage built earlier today, now that there are only two real destinations left instead of four). Rebuilt `dist/bold-paper-submission-guide.zip` without `internal-qa-review.html`.

### Folder structure tidied up; real homepage; README added

Repo root was flat — served pages, contributor docs, Firebase config, and the shippable zip all mixed together. Reorganized: served pages + Firebase config stay at root (GitHub Pages serves from root; `firebase` CLI expects its config there by default); the six contributor-facing docs (`AGENTS.md`, `DIFF.md`, `TODO.md`, `FIREBASE.md`, `changelog.md`, `proposed-boldiquette-addition.md`) moved into `docs/`; the shippable bundle moved into `dist/`. One real cross-file link needed fixing (`diff.html`'s `href="DIFF.md"` → `href="docs/DIFF.md"`) — everything else referencing these docs was prose, not links, so nothing else broke. The zip-build command now uses `zip -qj` (flatten) so the shipped bundle's internal layout stays flat regardless of the source reorg — recipients don't need to know about `docs/`.

`index.html` rewritten from a bare redirect into an actual homepage — links to the guide, Internal Review, the board, and the diff page, in the same design system as the rest of the suite. Added to the zip bundle (wasn't before). Added a root `README.md` (didn't exist) pointing at `docs/AGENTS.md` for the real contributor guide. Along the way, fixed several places in `docs/AGENTS.md` that had gone stale across earlier changes this session — it still described the Internal Review Board as `localStorage`-backed (it's been Firestore since Phase 1a), the reviewer picker as free text (it's a real roster `<select>` now), and card-level `track`/deadline-override/submitted-by as editable fields (all removed). Deleted a stray, already-gitignored `firestore-debug.log` from the working directory.

**Discovered GitHub Pages was actually broken since the `bold-lab-ai` transfer** — neither this commit nor the previous one (read-gating) had triggered a rebuild; the live site was serving a 404. Root cause: `bold-lab-ai` is on GitHub's Free plan for *organizations*, which — unlike a personal Free account — doesn't support Pages on a private repo at all (not even forced-public; just disabled). Re-verified the full git history for anything sensitive before acting (no secrets found anywhere; no real emails besides Eduardo's own, which is fine — it's just git authorship/an already-public project-owner note), then made the repo public — this was already the plan the hardcoded-role-list redesign existed to enable, see the "Role lists moved out of firestore.rules" entry earlier. Manually triggered a Pages rebuild afterward; confirmed live and serving the current commit.

### Board reads now require sign-in too; repo transferred to bold-lab-ai; notification system planned, not built

`firestore.rules`: `boards`/`cards` read now requires `isLabMember()` (was `allow read: if true`) — a real behavior change, not a no-op, since Sign-in-with-Slack is live. `audit-board.html`'s init flow now waits for the first auth-state resolution before loading anything (`loadInitialData()`), showing "Sign in with Slack to view the Internal Review Board" instead of a doomed fetch when signed out; signing out mid-session clears in-memory board/card data and backs out to the venues list. "+ New venue" disables when signed out, same pattern as "+ Register paper." Footer copy fixed (was still claiming "no sign-in yet").

Filed [`ml-conference-cycle#1`](https://github.com/bold-lab-ai/ml-conference-cycle/issues/1) for Slack DM notifications instead of building it blind — it needs the project's first Cloud Function (a Bot token can't live in client-side source, so something server-side has to hold it and make the `chat.postMessage` call), and the actual trigger events (reviewer-assigned? status-change? PI-approval? comments?) still need deciding.

Repo transferred from `epignatelli/ml-conference-cycle` to the `bold-lab-ai` GitHub org (Eduardo is an org admin). Pages URL changed as a result — was `http://epignatelli.com/ml-conference-cycle/` (Eduardo's personal verified domain), now `https://bold-lab-ai.github.io/ml-conference-cycle/` (the org has no custom domain verified, falls back to the default `github.io` Pages URL — also now genuinely HTTPS). Local `origin` remote updated to match; push confirmed working post-transfer.

### Subscribable calendar feed — Cloud Storage, not a Cloud Function

Closed the long-standing "Subscribable deadline feed" TODO item, unblocked now that there's a real backend. Chose Cloud Storage over a Cloud Function (client already builds the `.ics` bytes for the download button — Storage just needs those same bytes published at a stable URL, no server compute needed; would have been the project's first Cloud Function otherwise). `syncVenueIcs()` uploads to `venues/{boardId}/feed.ics` whenever a venue is created/edited, deletes it if the venue's deadline is cleared. New `storage.rules`: public read on that path (the point — calendar apps fetch it unauthenticated), write restricted to PI/admin via a `firestore.get()` cross-reference to `roles/pis`/`roles/admins` (Storage rules can't call Firestore rules' functions directly). Public URL is built deterministically from the bucket + path, not via `getDownloadURL()`'s token mechanism, since the object is genuinely public.

Setup hit two real snags, both now documented in `FIREBASE.md` in case they recur: (1) enabling Storage via Console created a bucket named `bold-d7ff2`, not the `bold-d7ff2.firebasestorage.app` the project's own config expected — `firebase deploy --only storage` gave a misleading "not set up" error for what was actually a bucket-name mismatch, confirmed via direct GCS API calls (404 vs 401 telling apart "doesn't exist" from "exists, not public") and fixed by selecting the correctly-named bucket in Console directly; (2) `match /venues/{boardId}.ics` doesn't compile as Storage Rules syntax — a wildcard can't share a path segment with literal characters — fixed by using a subfolder (`venues/{boardId}/feed.ics`) instead.

UI: a **Subscribe to calendar** button sits next to the existing **Add to calendar (.ics)** download button. Clicking it copies the URL and shows a small tooltip anchored to the button with instructions, rather than the page's normal toast — the toast region sits in normal document flow and shifts everything below it when it appears/disappears; a tooltip anchored to the button doesn't move anything else on the page.

### PI removed from the board card face

Dropped the "PI Name" line (`.card-pi`) from the compact card shown on the board — per Eduardo's request. Still shown on the detail page (full author list, last tagged PI), and `cardPi()` still backs the PI search filter; only the board-card display line and its now-unused CSS are gone.

### Registration form now uses real identity; reviewer picker uses the real Slack roster; track + per-card deadline override removed

Pulled the full Slack workspace roster (274 real, non-bot members with emails) via `users.list` (new `users:read`/`users:read.email` Bot Token scopes on the Slack app, reinstalled) and synced it into a new `people` Firestore collection (one-off Admin SDK script, same pattern as `roles` — not automated). `firestore.rules` updated: `people/{personId}` readable by any signed-in lab member, write-denied to clients. Sizing question flagged and confirmed with Eduardo before syncing (274 is the whole Slack workspace, not just BOLD-lab-internal — confirmed that's the intended roster).

`audit-board.html`: the paper-registration form no longer asks for "Corresponding author email" or "Submitted by" — both are set automatically from the signed-in Slack user (`state.currentUser`). Registering now requires being signed in (**+ Register paper** disabled with a tooltip when signed out; `onAddCard()` itself guards too). `submittedBy` is no longer editable after registration — it's what Security Rules authorize card edits against, not free-form metadata. This closes the gap flagged in the previous entry: `submittedBy.email` is now a real address, so the per-card `update` rule ("creator, assigned reviewer, or PI/admin") actually works for non-admins, not just `hasFullWrite()`.

Reviewer assignment (Junior/Senior) is now a `<select>` over the real roster (`people`), storing the person's actual email — not a free-text input matched against whatever names happened to appear on other cards. Authors (and PI, its last entry) stay free text with roster-backed suggestions, since a paper can have co-authors outside this Slack workspace.

Removed per Eduardo's request: **card-level `track`** (a badge + free-text field) and the **per-card deadline override** — a card now always inherits its venue's deadline. Rationale: a paper on a different track, or a workshop, gets registered as a *different venue* going forward, so a per-card override or track label no longer has a purpose. `effectiveDeadline()` simplified accordingly; the "venue default" vs "own override" distinction on the detail page is gone too, since there's only one source now.

`FIREBASE.md` and `TODO.md` updated to match throughout (data model, Security Rules section, the now-closed "real people picker" TODO item).

### Sign-in-with-Slack tested end-to-end — works; found and fixed a real save bug it exposed

Eduardo created the Slack app (Sign in with Slack, `openid profile email` scopes) and configured the `oidc.slack` OpenID Connect provider in Firebase Console (issuer `https://slack.com`, redirect `https://bold-d7ff2.firebaseapp.com/__/auth/handler` — deterministic from the project's `authDomain`, didn't need to be read off the Console UI). Split `audit-board.html`'s single emulator flag into two (`FIRESTORE_USE_EMULATOR`, `AUTH_USE_EMULATOR`) so real Slack sign-in (needs the live Auth backend — the emulator has no `oidc.slack` provider) could be tested while keeping Firestore writes local. Served locally via `python3 -m http.server`, tested by hand (this needs a real interactive Slack login, not headless automation): sign-in popup completed, `#authRegion` showed a real name — but no email, which turned out to be a UI bug, not a missing claim (the markup only ever showed name *or* email, never both). Fixed to show both.

With that fixed, tried registering a paper and got "Could not save the paper." Root-caused via the emulator's raw Firestore data (ground truth, not the app's own re-render): `saveBoard`/`saveBoardsIndex` re-`.set()` **every** document in their array on every call, which Security Rules treat as an `update` on pre-existing documents — so adding one new card also re-touched an unrelated pre-existing test card, whose own per-document authorization check failed and took the whole batch down. This isn't just a local-testing artifact: it would have broken real usage in production the first time any board had more than one person's cards on it. Fixed both functions to diff against the freshly-fetched Firestore snapshot (canonical, key-order-independent `stableStringify`) and only write what actually changed. Re-tested: old card left untouched, new card added successfully, confirmed via the emulator's raw data.

`FIREBASE.md` updated to match throughout.

### Admins role added; roles/pis + roles/admins seeded with real emails

Added a second full-write role, `admins` (`roles/admins` in Firestore, same shape as `roles/pis`), alongside PIs — not folded into `pis()`, since the lab treats them as distinct roles even though both get identical permissions via a shared `hasFullWrite()`. `firestore.rules` updated and redeployed to `bold-d7ff2` (verified live).

Seeded `roles/pis` (6 emails) and `roles/admins` (3 emails) with Eduardo's actual lists. No Firestore-data-write tool was available (see prior entry), so this needed an Admin SDK credential: Eduardo generated a service-account key via Firebase Console, wrote a one-off Node script (`firebase-admin`) to set both documents and read them back as verification. The downloaded key landed inside this git repo's own folder — caught before any `git add` (confirmed via `git status`, still untracked), moved out to a scratchpad directory, used from there, then deleted immediately after. Eduardo was asked to also revoke the key itself in Firebase Console, since the file being gone doesn't revoke the underlying credential. `roles/seniors` and `roles/juniors` remain unseeded.

### Repo pushed to GitHub, Pages deployed; role lists moved out of firestore.rules and deployed

Created a private GitHub repo (`epignatelli/ml-conference-cycle`), pushed this folder's git history, added `index.html` (redirects to `how-to-submit-a-paper.html`) so the site root resolves, and enabled GitHub Pages — live at `http://epignatelli.com/ml-conference-cycle/`. Found the account's plan doesn't support private Pages, so the rendered site is public regardless of the repo's own visibility; Firestore itself stays effectively deny-all so no live data is exposed either way (see below).

Decided board/card writes should be sign-in-gated (creator, assigned reviewer, or PI — matches the already-drafted rules). While deciding whether the repo could go public, realized the original hardcoded-role design (`pis()`/`seniors()`/`juniors()` as literal email arrays in `firestore.rules`) would publish real BOLD members' email addresses in git history if the repo ever went public. Moved the lists out of the rules file into three Firestore documents (`roles/pis`, `roles/seniors`, `roles/juniors`, each `{emails: [...]}`), referenced via `get()` inside the rules — `roles/{roleId}` itself stays permanently deny-all for clients, writable only via the Firebase Console. **Deployed** this ruleset to `bold-d7ff2` (verified live, matches the file on disk exactly) — a no-op in practice today since nobody can sign in yet, but the mechanism is real and live. The `roles/*` documents themselves don't exist yet — no credentialed path to write Firestore data available in this environment (no service account, no `gcloud`), so that step is manual, via Console, once Eduardo provides the actual email lists.

### Permission plan simplified for MVP; Sign-in-with-Slack UI scaffolded; git repo initialized

Eduardo scoped down the permission-layer plan for a quick MVP: Google Workspace Groups + Admin SDK + Cloud Function role lookup (the original Phase 3 plan) is deferred, not built now. Slack sign-in establishes identity only ("who am I"); role (junior/senior/PI) becomes a hardcoded email allowlist in `firestore.rules`, manually edited to promote/demote. `FIREBASE.md` rewritten to match: phasing renumbered, the Security Rules draft rewritten around `pis()`/`seniors()`/`juniors()` array functions instead of custom-claim groups, Cloud Functions marked deferred-to-later, "Inputs needed" reorganized with Blaze marked done and a concrete Slack-app-creation walkthrough added.

`firestore.rules` itself is **not** switched over yet, and stays literal deny-all — while writing the target rules, found that `submittedBy`/`reviewers.junior`/`reviewers.senior` are still free-text names (a "stand-in for Slack login" field and the shared people-autocomplete), not real emails, so an `request.auth.token.email`-based rule can't match anything real today. That's now documented in `FIREBASE.md` as the concrete prerequisite before the hardcoded-role rules can actually be deployed.

Added Sign-in-with-Slack UI scaffolding to `audit-board.html` (not yet functional — blocked on the Slack app + a manual Firebase Console step, both still pending): `firebase-auth-compat.js` script tag, `firebase.auth()` initialized against the Auth emulator, a new `#authRegion` in the masthead showing a "Sign in with Slack" button or the signed-in user's name via `onAuthStateChanged`, and a `signInWithPopup(new firebase.auth.OAuthProvider('oidc.slack'))` call with `openid profile email` scopes that fails gracefully (toast, not a crash) until the `oidc.slack` provider exists.

Also: Blaze (pay-as-you-go) plan upgraded on `bold-d7ff2`. Local git repo initialized in this folder with **no remote**, `.gitignore` added (emulator debug logs, `_test_*` scratch fixtures), and the Phase 1a Firestore swap committed.

### audit-board.html now runs on Firestore, not localStorage — Phase 1a shipped

Rewired `loadBoardsIndex` / `saveBoardsIndex` / `loadBoard` / `saveBoard` / `deleteBoardData` to talk to Firestore (project `bold-d7ff2`) instead of browser `localStorage` — same five-function seam, every other caller in the file unchanged. `boards/{id}` holds venue metadata; each card is now its own document in `boards/{id}/cards/{cardId}` (checklist/comments/discussion/history still embedded on the card for now — splitting those into their own subcollections for finer-grained concurrency is a deliberately deferred follow-up, see `FIREBASE.md`). Loads the Firebase compat SDK from `gstatic.com` (pinned `10.14.1`) via the `AGENTS.md` CDN exception. Targets the Firestore *emulator* by default (`FIRESTORE_USE_EMULATOR = true`) — the deployed cloud rules stay deny-all until there's a deliberate call on when to open them, since this is genuinely public cloud infrastructure now, not `localhost`. Verified end-to-end against the emulator: venue create/persist/reload, card register/persist, and a checklist tick all confirmed landing in real Firestore documents (the last one checked directly via the emulator's own data browser, not just the app's re-render, after a couple of false negatives from headless-Chrome testing artifacts — see `FIREBASE.md` for what actually worked). Footer copy updated (was "Data is saved to this browser only," now accurately says the board is shared with no sign-in yet).

### Firebase project actually stood up

Discovered/confirmed the Firebase MCP toolset (`mcp__plugin_firebase_firebase__*`) is available and already signed in. Found a Firebase project (`bold-d7ff2`, "BOLD") and a registered web app (`internal-review-board`) already existed — Eduardo had done the console setup from the earlier walkthrough. Pulled the SDK config directly via the tool rather than manual copy-paste. Firestore is enabled and deliberately left at `allow read, write: if false` (deny-all) — Phase 1 has no auth yet, and this is a real public cloud project now, so building/testing happens against the Firestore emulator instead of opening the live database. Ran `firebase_init` for Firestore: created `firebase.json`, `firestore.rules` (mirrors the deployed deny-all), `firestore.indexes.json` (empty, no composite indexes needed yet), and fixed `.firebaserc` (came out as an empty scaffold, added the `bold-d7ff2` default project alias by hand). All recorded in `FIREBASE.md`, which is now also in `AGENTS.md`'s file table alongside the new Firebase project-config files (kept separate from `bold-paper-submission-guide.zip` — that bundle is the shippable guide, not the app's infra).

### Firebase backend — design phase started

New `FIREBASE.md`: the concrete plan for moving off per-browser `localStorage` onto Firestore + Identity Platform (Slack sign-in) + Firebase custom claims (Google Workspace Groups for eligibility, `reviewers.junior`/`reviewers.senior` field checks for per-card authorization) — the resolved design for what `TODO.md`'s "Shared storage" and "Permissions" sections had been describing as requirements. Normalized the data model further than originally sketched: checklist items and both comment thread types (per-item feedback, card Discussion) become their own subcollections with a flat `parentId` field, so ticking a checkbox or posting a comment never needs a transaction or contends with another reviewer's write — only the card's own mutable fields (status, reviewers, edit-fields) need the version-checked transaction. `TODO.md` cross-references `FIREBASE.md` rather than duplicating the plan; `AGENTS.md`'s file table and bundle-rebuild command updated. No code wired up yet — waiting on a live Firebase project (the only current blocker) plus, in parallel, the Slack app and Google Workspace delegation for later phases.


### Front-end redesign to match bold-lab.ai

Full visual pass so the three pages read as a native part of `https://bold-lab.ai/`. Design tokens lifted from that site's own stylesheet; all JS and page logic unchanged.

- **Typography: Inter only** (was Fraunces display + Inter body). Headings at `font-weight:760`, `letter-spacing:0`, tight leading — bold-lab.ai's exact heading style. Loaded via the Inter variable axis (`wght@400..800`).
- **Palette** adopted verbatim from bold-lab.ai: `--ink:#09111f`, `--paper:#fbfaf6` (warm near-white page), `--surface:#fff` (cards/columns/inputs/modals now lift off the page), `--line:#d8dee6`, accents `--blue` (links/primary), `--teal` (Format, eyebrows), `--plum` (Science), `--amber` (attention). **Focus ring is amber** (`--focus:#c87912`), as on bold-lab.ai.
- **8px radius** on cards, columns, buttons, inputs, badges, modals (pills stay 999px) — was zero-radius everywhere.
- Eyebrows / small labels: uppercase but **not letter-spaced**, coloured teal — matching bold-lab.ai. Dropped middot-joined meta strings (masthead subs, footers, the outline "then:" line) and trailing `→` on resource links (kept on directional controls: pager, card Left/Right).
- **Guide:** the `01–09` phase numerals are replaced by the **week countdown** — `8w … 0` in the TOC (with an "after submission" divider before the un-numbered post-submission phases), and a `−8 wk … deadline` pill on each phase head. The redundant "N weeks before the deadline" sentence leads were removed from phase intros. The standalone schedule table under the title is **removed** — the left menu now carries the countdown; a one-line note keeps the "deadlines count back from the venue's paper deadline; the board derives the dates + .ics" point. Blockquote de-serifed.
- **Internal Review:** Format = teal, Science = plum — the two-part structure is now carried by colour (tab underline, part rule, checkbox `accent-color`). Dropped the `01/02` part numerals and the italic dek.
- **Internal Review Board:**
  - Page on warm `--paper`; columns, cards, venue rows, inputs on white `--surface`. Column headers get a `--wash` fill and a `min-height` so card columns line up regardless of hint length.
  - Card checklist matrix: **Author = blue, Junior = teal, Senior = plum** column headers and checkboxes; Format/Science group headers coloured to match. Row hover.
  - Gate banner: amber (pending) → teal (complete).
  - Cards decluttered — the per-card status `<select>` is gone (moving is Left/Right on the card, or the dropdown on the detail page; the select still appears on an orphaned-status card so it can be rescued). Card footer is just edit / remove.
  - **Venue "Deadlines" is now a horizontal timeline** — one dot per milestone on a rail (`vs-rail`), even-spaced, with a short name + short date under each. Derived milestones = blue-ring dots, the venue's own dates = muted-ring, the paper deadline = a filled ink dot. Replaces the 12-row label/date/note table; far more compact. `internalSchedule` rows gained a `short` label; `formatDateShort` added. `.ics` export and `internalSchedule` data unchanged.
  - Board **column headers are now a uniform height** (`min-height:76px` + `.ch-hint` clamped to 2 lines), so every column's cards start at the same line regardless of hint length.
- **Guide gets the same timeline** — a horizontal `.timeline` rail under the title: 9 phase dots (blue = pre-submission with week markers, filled ink = Submit, muted = post-submission), each a link that jumps to that phase and stays in sync with the left index and the pager. Hidden on mobile (the TOC becomes a horizontal strip there).
- `TODO.md`: new "Explore: run Internal Review on OpenReview" section — use a private OpenReview venue for the actual review instead of reimplementing review threads / rebuttal / decisions on the board.
- **Checklist: dropped the Author column.** The matrix is now two passes — **Junior** and **Senior**. `REVIEW_ROLES = ['junior','senior']`, `makeChecklistSnapshot` items are `{…, junior, senior}`, the Approved gate needs both at 34/34. `normalizeCard` migrates every older shape (single `checked`, `author`/`auditor`, `author`/`junior`/`senior`) → `junior`/`senior`, dropping `author`.
- **Assign reviewers per submission.** New `card.reviewers = { junior, senior }` (names, free text for now). Replaces the unused single `assignedTo`. Gate banner nudges to assign when either is blank. Guide Phase 3, `internal-qa-review.html` lede, `DIFF.md` §1, and `proposed-boldiquette-addition.md` updated (two reviewers, no author self-check).
- **Card preview decluttered** — dropped due date, corresponding-author email, and "Submitted by" from the board card. Still on the detail page; just off the compact preview.
- **Reviewer assignment made prominent: inline inputs right on the card.** The old muted "Reviewers · Jr … · Sr …" text line is now two live `Jr` / `Sr` inputs on the card face (teal / plum labels, matching the checklist columns) — no modal needed, saves on blur. **Unassigned inputs are tinted amber** so a paper missing a reviewer stands out on the board at a glance. The same inputs replace the static text on the detail page. Both share one `<input list="reviewerNames">` autocomplete per venue, sourced from `reviewerPoolNames()` — every card's submitter + already-assigned reviewers, deduped — as a stand-in for the real roster (tracked in `TODO.md`). Removed the now-redundant Reviewers section from the card's Edit-fields modal (links/notes only there now).
  - Board columns 176px (was 190); board region widened toward viewport with scroll-snap. `.btn-text` hover fixed (was turning every text button red; now only Delete).
- Verified by headless-Chrome screenshots at desktop widths (guide, both review tabs, board venue view, card detail). Mobile layout uses the existing responsive rules; not re-verified on a device this pass.

- **Venue: explicit Pitch Day field** (`pitchDay`, optional — defaults to −6 wk). Abstract deadline label marked "(optional)".
- **Venue: "Reviews released" and "Rebuttal deadline" date fields** (`reviewsPublicDate`, `rebuttalDeadline`) — standard venue dates, not tagged optional. These, plus abstract / notification / camera-ready, join the derived pre-submission milestones in one chronological "Deadlines" table and the .ics — **each row shown only when the date is set** (venues announce post-decision dates late). Only Abstract / Notification / Camera-ready carry an "(optional)" label; the modal note says to add the rest as they're announced. The "…plus the venue's own dates" clause in the table header appears only when there are any. The old separate notification/camera-ready line in `venueMetaHtml` is gone.
- **Board columns get a one-line "move a card here when…" hint under each title** (`STATUS_HINTS`), e.g. "First full draft is done" under *Ready for review*, "All three checklist passes complete" under *Approved*. `.column-head` restructured into a `.ch-top` row + `.ch-hint` line; `.columns` min-width bumped for the now-11 columns.
- `.field-error` (the "Please fill in the required fields above." / "Venue and year are both required." messages) is now a red-bordered, light-red box instead of a bare red line.
- **Submission system is now a link.** `submissionSystem` holds the URL of the venue's submission page (e.g. OpenReview); the venue header shows it as "Submission page →" next to "Call for papers →", not as a plain rules token.
- **Venue board now shows a clear "Deadlines" table** — every internal milestone derived from the paper deadline (register −8 wk, Pitch Day explicit-or−6 wk, first draft −4 wk, final draft −2 wk, PI polish −1 wk), plus the abstract and paper deadlines, in date order, each with its date and how it's derived. Replaces the old cramped one-line "runway" in `venueMetaHtml`.
- **"Add to calendar (.ics)" button** on the venue board — downloads all those milestones as all-day VEVENTs with stable per-milestone UIDs (`internalSchedule` / `buildIcs` / `downloadIcs`). Guide Phase 1 mentions it; a hosted subscribable `webcal://` feed is a TODO (needs a backend).
- Guide: replaced the nine per-phase "Source: BOLDiquette §…" lines with **one page-level source line under the title** — "Source: BOLDiquette § Pre-submission · current differences in DIFF.md". (Phases 6–9 also drew on § Post-submission / § Arxiv / § Publicity / § ML Conference Cycle — those anchors now live only in `DIFF.md`.) `.phase-source` CSS renamed `.page-source`.
- **Card detail page audited for completeness against the card preview and the data model.** Everything on the card face was already on the detail page (due date/email/Submitted-by had already moved there); found two things the data model captures but nothing ever showed: `card.createdAt` (registration date) and `card.history[0].note` (the free-text note entered at registration). Both now render in the detail grid — "Registered" and "Note at registration" — via a new `formatTimestamp()` helper. Also: the Deadline row now says "(venue default)" when it's inherited from the venue rather than set on the card, and a card stuck on a status that's no longer a column gets an explicit warning banner on the detail page (the card preview already had one).
- **Overleaf link is now editable after registration.** It was set once at `pOverleaf` on the registration form and then fixed for the card's lifetime — Submission link and Rebuttal doc could be changed later, Overleaf couldn't. Added to the Edit-fields modal (`eOverleaf`, first field, saved in `onSaveEdit`).
- **Overleaf link shown in full on the detail page**, not hidden behind a generic "Open" link — was invisible at a glance (had to hover/click to know where it pointed).
- **Overleaf link is checked for edit access, at registration and on edit.** New `overleafLinkIssue()` rejects `overleaf.com/read/…` (read-only share links) and any other `overleaf.com` URL that isn't `overleaf.com/project/…`, with an inline error explaining reviewers need edit access and pointing at the address-bar URL / Share → invite-as-editor. Non-overleaf.com URLs (e.g. a self-hosted mirror) pass through unchecked.
- **Guide hero gets a visible "Open the Internal Review Board" link** (`.hero-cta`, its own line under the hero note, bold text — no button chrome) — was only a mid-sentence text link, easy to miss on a page whose whole job is routing people to the board.
- **"Source:" relabelled "Provenance:"** on the guide's page-source line.
- **New `diff.html`** — a styled, rendered page for `DIFF.md`'s content (legend badges, per-item BOLDiquette quote / Implementation / Proposed-change structure, a numbered jump list, the "Known limitation" callout, the Reconciled section), matching the site's design system. The "current differences in DIFF.md" link on all three pages now opens this instead of the raw Markdown file (which is still linked from `diff.html`'s own provenance line for anyone who wants the source text). No build step — `diff.html` is hand-kept in sync with `DIFF.md` (see `AGENTS.md` guideline 1); added to the zip bundle.
- **Board columns rebuilt to mirror the guide's 9 phases, one for one.** The old 11 statuses (Writing in progress → Ready for review → Changes Requested → Approved → PI polish → Submitted → Rebuttal in progress → Rebuttal submitted → Accepted / Rejected / Withdrawn) encoded a review workflow that didn't line up with the guide's time-boxed phases; the board now runs **Register → Pitch Day → Draft & Review → Final Draft → PI Polish → Submit → arXiv & Publicity → Rebuttal → Pre-Conference** — same names, same order as the guide's `.timeline`. Two new phases the board never tracked before (Pitch Day, arXiv & Publicity) are now real columns.
  - Sub-states that used to be their own column are now a **badge + colour on the card** instead: Draft & Review collapses the old Ready-for-review/Changes-Requested/Approved split into one column, colour-coded green (Approved, both checklist passes complete) / amber (In review, the default) / red (Changes requested) — `reviewState(card)`, a reviewer's explicit flag always winning over the checklist. A **"⚑ Flag changes requested"** toggle (card face + detail page) sets it; clears itself back to "✓ Clear…" once pressed again. Rebuttal similarly collapses in-progress/submitted into one column, badged from whether `rebuttalDocLink` is set — no new field needed.
  - **Accepted / Rejected / Withdrawn are outcome badges now, not columns** (`card.outcome`, green/red/grey) — they aren't phases in the guide's timeline either. Set from the **Edit fields** modal (`eOutcome`), shown as a badge wherever the card currently sits.
  - The **Approved gate moved**: `GATE_STATUS` is now `final_draft` — a card can't leave Draft & Review for Final Draft until both checklist passes are complete (previously the same rule, targeting the old `approved` column).
  - **Migration:** `STATUS_MIGRATIONS` remaps every old status key to its new column; where the old status carried information a rename alone would lose, `normalizeCard()` also sets the new field — `changes_requested` → `draft_review` + `changesRequested: true`; `accepted`/`rejected`/`withdrawn` → their nearest column (Pre-Conference / Rebuttal / Submit respectively) + the matching `outcome`. Existing boards migrate on load with no data loss.
  - `.columns` `min-width` recomputed for 9 columns (1594px, was 1948px for 11).
- **"PI Polish" renamed "PI Approval"** (board column, guide Phase 5, venue-schedule milestone label — the `.ics` UID key `pi_polish` is unchanged, only display text). It was unclear whether a card belonged there while the PI's review was in progress or only once done — the whole point of the column is the PI's sign-off, so naming it for the completed checkpoint (not the ongoing "polish" activity) settles that. Rewrote the `final_draft` and `pi_polish` `STATUS_HINTS` the same way — both used to describe an in-progress action ("comments incorporated", "in the PI's final-week review") where the column actually means the *entry* criterion is met.
- **Advancing a card now asks for a brief confirmation.** Every forward move ("Advance", not "Revert" — see below) through any of Left/Right, the status dropdown, or the detail page shows a `window.confirm()` built from a couple of bullet points specific to the destination column (e.g. moving to Pitch Day: "Confirm the paper was pitched to the group at Pitch Day."; PI Approval: "The PI has read the final draft." + "The PI has approved submitting it."). Cancel leaves the card where it was. Nothing shown on Revert (moving backward) or on Register (the starting column, never a move target). New `STATUS_ADVANCE_CHECKLIST` map + `advanceConfirmMessage()`, wired into `onChangeStatus()`.
- **"Left"/"Right" renamed "Revert"/"Advance"** on both move buttons (card face + detail page), matching the attestation framing above — "Advance" reads as a deliberate forward step, not a neutral direction.
- **Board columns widened** (176px → 200px → 240px each; `.columns` min-width now 2170px) — more room for card content and the column hints.
- **Board card slimmed down.** Dropped the track badge, the "View on Overleaf" link, and the edit / remove (✎ / ×) footer icons from the card face — all still reachable on the detail page (track in the grid; "Edit fields" and a new "Remove paper" button in `.detail-controls`). The `.card-meta-row` div is now omitted entirely when a card has no badges, rather than sitting empty. The card title is underlined so it reads as the click target. Net effect: a resting card is title → PI → reviewers → checklist progress → Revert/Advance, several rows shorter. Removing a paper from its detail page navigates back to the board.
- **Every card now has a PI and a list of authors.** `card.pi` (required, single name) and `card.authors` (required, at least one) — both plain text for now, same stand-in-datalist treatment as reviewers, with an explicit TODO for the real roster once it exists. Authors are a dynamic list in the registration form: "+ Add author" appends a row, each removable, values preserved across add/remove (`pAuthorsState`, `authorRowsHtml()`, `renderAuthorRows()`). PI shows on the card face (compact, for scanning a big board) and both show on the detail page; neither is editable after registration (same precedent as title/track/email).
  - Generalized the old reviewer-only autocomplete into one shared roster: `reviewerPoolNames()` → `peoplePoolNames()` (adds `pi` + `authors` to the union), `reviewerDatalistHtml()` → `peopleDatalistHtml()`, datalist id `reviewerNames` → `peopleNames`. PI, author, and reviewer inputs all point at the same list now, so a name typed anywhere suggests everywhere.
- **Search and filter the board.** A search bar (title/PI/author/reviewer/submitter, or scoped to just Title/PI/Authors via a select) filters every column live as you type, with a match count and a distinct "No matches" vs "Nothing here" per column — the thing that was getting hard once a board has a lot of papers on it. `matchesSearch()` / `boardSearchHtml()` / `onSearchInput()` (the last preserves the search box's focus and cursor position across the re-render its own typing triggers).
- **Checklist consolidated 34 items → 12** (Format 19→6, Science 15→6). Two moves: sibling checks inside a category were each merged into one longer line (e.g. the four Double-blind-anonymity items became two; the three Template & formatting items became one), and the **Submission-system category was dropped entirely** — that's what the venue's own submission form covers, not something the draft needs an internal check for. A reviewer now ticks 12 boxes per pass instead of 34, cutting a full junior+senior review from 68 clicks to 24. `CHECKLIST_TEMPLATE` and `internal-qa-review.html`'s `c*`/`s*` checkboxes rewritten in lockstep; the standalone page's `localStorage` key bumped (`-v2` for the consolidation, `-v3` for the Submission-system drop) so stale ticks don't land on renumbered items. `normalizeCard()` rebuilds a stored card checklist whose length/labels no longer match the template, carrying a tick forward only where a role had *fully* finished that part (partial progress restarts against the shorter list). Count-derived UI already reads totals off the card, so the gate banner just says "junior 0/12, senior 0/12" with no code change.
- **Per-checklist-item feedback threads.** Under every checklist row there's now an "Add feedback" toggle (turns into "💬 N comments" once used); expanding it opens an inline OpenReview-style thread pinned to that one point — free-text name + comment, one level of replies, exactly like the Discussion tab (shares `discussionMessageHtml` / `discussionFormHtml`). Meant for "watch out, your name is visible on p.4 line 312" style notes that anyone can then reply to. New per-item `comments` array (`{ id, author, timestamp, body, replies }`); `onPostChecklistComment()` / `onPostChecklistReply()`; `state.checklistCommentOpenId` / `state.checklistReplyOpenId` (only one thread + one reply box open at a time). **Comments don't gate the checkbox** — `checklistComplete()` still reads only the junior/senior ticks; an open concern is a nudge for a human, not a hard block. `normalizeCard()` defaults `comments: []` onto older stored items.
- **Card detail page's review area split into three tabs: Format, Science, Discussion.** The checklist matrix used to be one long list with a Format/Science divider partway down (`.cl-part`); it's now two tabs, each showing just its own part's items (`checklistTabHtml(card, part)`), styled after `internal-qa-review.html`'s own Format/Science tabs (teal/plum underline). New third tab: **Discussion**, an OpenReview-style thread on the card — free-text name + message, post a new topic or reply to an existing one, **replies capped at one level of nesting** (a reply has no `replies` field of its own, so there's structurally no way to reply to a reply). `card.discussion`: array of `{ id, author, timestamp, body, replies }`; `onPostDiscussion()` / `onPostReply()` append and save, rejecting an empty name or body with a toast rather than posting silently-blank. Only one reply box is ever open at a time. The Discussion tab shows a running count (top-level + replies) next to its label. `state.detailTab` tracks which tab is active, reset to Format whenever a card is opened.
- **Edit fields now actually covers all the fields.** The modal used to be links-and-notes only (Overleaf, submission link, rebuttal deadline/doc, review notes, outcome); title, authors, corresponding-author email, track, compute estimate, deadline override, and submitted-by were registration-only. All of those are now in the Edit-fields modal too, same required set and same validation (`overleafLinkIssue()`, the PI-is-the-last-author check) as registration. Authors reuse the same dynamic add/remove-row component as the registration form — generalized `renderAuthorRows(wrapId, authorsArr, onEnter)` to take a target wrap id, backing array, and Enter-to-submit callback, so both modals share one implementation with their own state (`pAuthorsState` / `eAuthorsState`). Reviewers, status, and the checklist stay out of the modal on purpose — those already have faster, more direct ways to edit them.
- **Column hints now describe what's happening, not what already happened — the opposite tense from the column title on purpose.** The title says which checkpoint was met to land here; the hint says what's actually going on with the paper right now (mostly "waiting for the next one"): Registered → "Waiting for pitch", Pitched → "First draft in writing", Drafted → "Waiting for internal review", Reviewed → "Waiting for PI approval", PI Approved → "Waiting for official submission", Submitted → "Waiting for arxiv and publicity", Posted → "Waiting for official review", Reviews Out → "Rebuttal in process", Accepted → "Planning for conference".
- **Board columns renamed past tense — a name is the checkpoint already met, not the activity in progress.** Register → Registered, Pitch Day → Pitched, Draft & Review → Drafted, Final Draft → Reviewed, PI Approval → PI Approved, Submit → Submitted, arXiv & Publicity → Posted, Rebuttal → Reviews Out, Pre-Conference → Accepted. The guide's own phase headings are unchanged (they're instructional — "Pitch Day", "Submit" — telling you what to go do, not asserting it's done); only the board's `STATUS_LABELS` moved, same 9 phases and order either way. Also: the last column being named "Accepted" would double up with the existing Accepted *outcome* badge on any card sitting there, so `shouldShowOutcomeBadge()` now suppresses that one specific redundant case (Rejected/Withdrawn always still show — no column is named after them).
- **New hard gate: can't Advance from Pitch Day into Draft & Review without both reviewers assigned.** The Advance button greys out (title tooltip: "Assign a junior and a senior reviewer first") exactly like it already did for the Draft & Review → Final Draft checklist gate, and the same rule is enforced in `onChangeStatus()` regardless of which control tried to move the card (Advance, the status dropdown, the detail page). Only blocks the forward direction — reverting a card back into Draft & Review is never blocked by it. Generalized the single-purpose checklist-gate check into `advanceBlockReason(card, toStatus)`, returning whichever reason applies (checklist first, then reviewers), used by both the render side and `onChangeStatus`. Also fixed the gate banner's stale "(Edit fields)" pointer for assigning reviewers — that's been inline on the card/detail page for a while now.
- **PI folded into Authors — one field, not two.** Dropped the separate `card.pi`; **the PI is now just the last entry in `card.authors`** (`cardPi()` reads it off), same as it usually is on the paper's own author line. The registration form's PI field is gone; the last row in the Authors list is tagged "PI", and "+ Add author" now inserts new rows *before* the last one so the PI stays put as co-authors are added, rather than getting bumped by a fresh blank row. `normalizeCard()` migrates any card saved under the short-lived separate-PI-field shape by folding `pi` into `authors` as the last entry (deduped if it was already there) and dropping the stray key. PI-scoped search now reads the last author instead of a separate field.
- Guide retitled **"ML Conference Paper Cycle"** (was "How to Submit a Paper") — `<title>`, `<h1>`, OG title, and the backlinks on the other two pages. Filename unchanged.
- Dropped the hero lede ("This page implements BOLDiquette § ML Conference Cycle…") and the "At a glance" heading above the runway table — table kept, now sits directly under the title with a hairline divider. Removed the now-dead `.hero .lede` and `.outline h2` CSS.
- **Restructured the whole process to the real pre-submission runway** (from Mattie, who ran this before). Weeks before the venue's paper deadline: **8** abstract + outline + compute estimate → registered · **6** Pitch Day · **4** first full draft → Internal Review · **2** final draft with all comments in · **1** PI polish · **0** submit.
- **Guide: 7 → 9 phases.** New phases: Register the Paper, Pitch Day, First Draft & Internal Review, Final Draft, PI Polish. Submit / arXiv & Publicity / Rebuttal / Pre-Conference shift down and renumber. Added an **"At a glance"** runway table above the phases. Fixed the arXiv phase's cross-reference (Phase 4 → 6).
- **Unified naming.** "Pre-Submission Audit" → **Internal Review**; "Audit Board" → **Internal Review Board**; "Auditor" / "Science Auditor" → **reviewer** (drawn from a pool). Applied across all three pages, `DIFF.md`, `TODO.md`, `proposed-boldiquette-addition.md`. Filenames, the `bold-audit-board:` storage prefix, and JS/CSS identifiers keep their old spellings on purpose.
- **Internal Review is a simulated full peer review** by a **junior** and a **senior** reviewer from the pool (all papers' authors + volunteers, ≥1 senior). `internal-qa-review.html` retitled and its lede/subs reframed.
- **Checklist gate: 2 → 3 passes.** Each card's 34 items now carry `author` / `junior` / `senior`; the detail page shows three checkbox columns; a paper can't reach *Approved* until all three are 34/34. `normalizeCard()` migrates the old author/auditor shape (old `auditor` → `senior`).
- **Board: new `PI polish` column** between Approved and Submitted (`pi_polish` key). Statuses are now Writing in progress → Ready for review → Changes Requested → Approved → PI polish → Submitted → Rebuttal in progress → Rebuttal submitted → Accepted / Rejected / Withdrawn.
- **Board: compute-estimate field** on the register-paper modal ("Register paper for review", was "Submit paper for audit"), shown on the card detail page.
- **Board: venue header shows the derived runway** — register / pitch / draft / final / PI-polish dates computed 8/6/4/2/1 weeks back from the venue's paper deadline (`weeksBefore`).
- `DIFF.md` reworked: §1 Internal Review (full runway + reviewer pool + 3-pass checklist), §4 now the whole timeline (replaces the old 10-vs-21-day item), new §5 (junior/senior reviewers, PI only at week 1), §6 compute estimate, §7 Pitch Day. Pre-Conference items renumbered to §9/§10. `proposed-boldiquette-addition.md` rewritten to match.

## 2026-09-10

- **Homepage now states it's an implementation of BOLDiquette.** The lede opens with "This is BOLD Lab's working implementation of BOLDiquette § ML Conference Cycle", link first; `meta`/OG descriptions updated to match.
- **Phase 1 "Log the project"** now points to the Audit Board instead of the project-tracker spreadsheet (divergence — recorded in `DIFF.md` §3).
- **Phase 3 first-draft lead time: 21 → 10 days**, to match current BOLDiquette. The case for 21 is kept as a proposed BOLDiquette change in `DIFF.md` §4. Recipient wording reconciled: BOLDiquette's "compulsory BOLD PI" is noted, with internal review widening it to the group via the Auditor / Science Auditor (`DIFF.md` §5).
- **Phase 5 arXiv affiliation** changed to "BOLD, {University of Oxford / Imperial College / UCL}", matching BOLDiquette exactly (was Oxford-only). No longer a divergence.
- **`internal-qa-review.html`: Format and Science split into two tabs** (JS, with a no-JS stacked fallback), mirroring the main guide's phase pager. `#format` / `#science` deep-links supported.
- `internal-qa-review.html`: `meta`/OG descriptions rewritten to cover both parts (were Format-only). Lede no longer claims the Audit Board gates the To Do column (it doesn't) — reworded to "mandatory before the paper is submitted to the venue".
- `internal-qa-review.html`: checkbox ticks now persist per-browser as a personal working copy, with a "Clear all ticks" control and a note that the tracked per-paper record lives on the Audit Board.
- **Added `DIFF.md`** — standing ledger of implementation-vs-BOLDiquette differences and proposed BOLDiquette changes.
- `proposed-boldiquette-addition.md`: resolved both `[CONFLICT]` markers (deadline → 10 days + proposal note; affiliation → reconciled).
- Homepage: `.hero` left-aligned with the phase content column (left padding 288px = layout padding + TOC column + grid gap), so the page description and every phase's description share one left edge.
- Removed the inline "differs from BOLDiquette" notes from Phase 1 and Phase 3 and the tracking clause from the hero lede — that commentary belongs only in `DIFF.md`. Tightened all page descriptions (meta/OG and hero ledes on all three pages).
- Phases 9 → 7. Dropped "On Acceptance" (BOLD OS will publish accepted publications, so the manual website-form step goes away). Merged "Release on arXiv" + "Publicity" into one phase with two subheads. All sections, the TOC, the pager, and the dek renumbered.
- Pre-Conference (now Phase 7): dropped the Google-Slides-only rule (HTML decks are fine, often agent-built); added "email people to meet 3 weeks before the conference" and "check the social events for networking".
- `DIFF.md` §6–§8 record the three new Pre-Conference / On-Acceptance divergences; `TODO.md` bidirectional-linkage item updated.
- Dropped the hero dek ("Seven phases, from picking a venue to standing at the poster") and its now-unused CSS — redundant with the TOC and lede.
- TOC entry 01 renamed "Choose & Organize" → "Choose Your Venue".
- Phase 4 title "Submit, Then Keep Working" → "Submit" (heading and TOC).
- Phase 5 intro now sequences arXiv: after submission and after the Phase 4 extensions plan, the next step is to post on arXiv.
- Every BOLDiquette source citation (all 7 phase-source lines + the two audit pages' page-source) now ends with "· current differences in DIFF.md", linking the file.
- Audit Board column "In Audit" → "Ready for audit" (label only; the `in_audit` status key is unchanged, so stored data is unaffected). Updated the matching references in the guide, DIFF.md, TODO.md, and proposed-boldiquette-addition.md.
- **Audit Board no longer depends on Claude.** Replaced the `window.storage` (Claude Artifacts) layer with browser `localStorage` — new `lsRead`/`lsWrite`/`lsRemove` helpers behind the same `loadBoard`/`saveBoard`/… functions. The "only works as a Claude artifact" disabled state is gone; the board now works opened as a file or hosted anywhere. Footer copy updated ("saved to this browser only"). `TODO.md` storage section reframed around the remaining need (a *shared* backend). Verified end-to-end over plain HTTP: create venue → add paper → survives reload.
- Added `AGENTS.md` — what the project is, how to run it, constraints, and editing guidelines. Bundle is now 8 files.
- Phase 7 (Pre-Conference) content split into "Poster" / "Talk" / "Networking" subheads. Social-events bullet made explicit: joining as many as you can is mandatory. DIFF.md §8 updated to match.
- Audit Board column "To Do" → "Writing in progress" (label only; `todo` key unchanged). Updated the guide, DIFF.md, TODO.md, proposed-boldiquette-addition.md. TODO.md "core change" section reworked: once BOLD OS is live this column is auto-populated from every publication attached to your projects, and the add-paper form goes away — you just promote a card to "Ready for audit".
- Audit Board cards get **← Left** / **Right →** buttons on one line, above the status row — move the card one column either way. Disabled at the ends; destination shown in the tooltip. The status dropdown stays for any jump.
- **Audit Board: card detail page.** Clicking a card title opens a full page for that paper — all fields, links, status controls, "Edit fields", and the 34-item checklist as **two passes, Author and Auditor**. A green/amber gate banner tracks both counts. `checklist` item shape changed to `{ id, part, category, label, author, auditor }`; `normalizeCard()` migrates older cards (status keys + the old single-`checked` shape) on load.
- **Approval gate.** A paper can't move to *Approved* (or any status right of it) from before it until both checklist passes are 34/34 — enforced centrally in `onChangeStatus`, with the board's "Right →" button disabled and a toast on a blocked dropdown jump. Board cards show a "Checklist · author X/34 · auditor Y/34" line. Restricting who can tick which pass is a TODO (needs identity). DIFF.md §1 and proposed-boldiquette-addition.md updated.
- `TODO.md`: added a **Permissions** section — no auth layer today; once Slack OAuth lands, every action requires sign-in, only the author / project owner / assigned Auditor may move a card, and each checklist pass is restricted to its role. Folded the old "who can tick which pass" / status-gate items into it (`can(action, card)` stub).
- **New-venue form is now a modal with far more fields** (was a 3-input inline strip): website / CFP, abstract · paper · notification · camera-ready dates, conference dates, location, max papers per author, page limit, anonymity, submission system, notes. Venue + Year still the only required fields. Added **Edit venue** (same modal, pre-filled). The venue header shows a metadata line; the venues list shows the location. `deadline` key kept as "Paper deadline" for compatibility. Field shape is documented in `TODO.md` for a future agent to fill from a call for papers.
- Audit Board "Rebuttal" column split into "Rebuttal in progress" and "Rebuttal submitted" (keys `rebuttal_in_progress` / `rebuttal_submitted`). `STATUS_MIGRATIONS` maps the old `rebuttal` key forward on load. Deadline/overdue logic now applies to "Rebuttal in progress" only. DIFF.md and proposed-boldiquette-addition.md status lists updated.
- Phase 2: cut the "feedback that repeats a point already in the guide lands badly — worth internalizing early" aside; kept "Read and closely follow the 'How to ML Paper' guide."
- **Added `TODO.md`** — work blocked on BOLD OS, chiefly: a publication can be added to the Audit Board only by selecting one from a BOLD OS project (no free-text entry). Also: permanent storage for the Audit Board (it currently persists only via the Claude Artifacts `window.storage` API — needs a real database + API).

## Earlier

*Traces the build of these files. Some entries describe states that later changed (e.g. phase counts, a "Failed" column, a standalone `science-audit.html`).*

- Merged "Submit" + "Post-Submission: Capture Loose Ends" into one phase, "Submit, Then Keep Working" (9 phases → 8, renumbered)
- First-draft deadline: 10 days → 21 days ahead of final deadline
- Added: draft now triggers internal QA review process — links to `internal-qa-review.html` (page doesn't exist yet)
- Added: line framing submission as a step, not a pause — keep working while awaiting review
- Named the internal QA step "Desk-Check", then "QA", then settled on "Pre-Submission Audit" — updated the link text in Phase 3 to match, and its description now states it's a quality assurance step
- Named the person who runs it "Auditor" — introduced in the audit page's lede
- Built `internal-qa-review.html` (Pre-Submission Audit page) — first-draft checklist covering mandatory sections/parts, page limits, double-blind anonymity, template/formatting, submission-system details, and policy compliance; still awaiting more detail
- Built `audit-board.html` — a live, shared kanban board (one per venue/year) for submitting papers and tracking audit status: To Do, In Audit, Changes Requested, Approved, Submitted, Withdrawn, Failed. Linked to it from the Pre-Submission Audit page.
  - Runs on the Claude Artifacts persistent-storage API — works when opened as an artifact, but shows a clear disabled state instead of breaking if hosted as a plain static file elsewhere (that setup has no backend, so it can't actually be "live" there)
- Implemented the agreed paper data model in the submission form:
  - Submitter provides: title, authors (list), Overleaf link, corresponding author email, track, optional deadline override
  - Auto-generated on submit: id, `submittedBy` (name only for now — placeholder until Slack login is wired in, so `slackId` stays null), status (`todo`), `assignedTo` (null), a full checklist snapshot copied from the Pre-Submission Audit page, a history log seeded with the "submitted" entry, `createdAt`/`updatedAt`
  - Board-level optional deadline added; a card without its own deadline inherits the board's
- Removed the authors field from the submission form and data model &mdash; not needed for now
- Fixed: clicking "Create" (new board) or "Submit" (paper) could silently do nothing. Both forms now use plain buttons instead of native form-submit, with their own validation and visible error messages; storage calls are also hardened against synchronous failures and errors are now surfaced instead of failing silently
- Restructured navigation into two separate views: a venues list (home view, no submit button) and a nested venue view (opened by clicking a venue) that holds that venue's "+ Submit paper for audit" button and kanban columns. Creating a venue now drops you straight inside it; deleting one returns you to the list.
- Built `science-audit.html` — a second, separate audit checking correctness and impact (not formatting). Structured around claimed contributions, correctness, impact, limitations, and related-work positioning, drawn from RLC/NeurIPS/ICML/ICLR's actual reviewer criteria. Role: "Science Auditor." Linked from Pre-Submission Audit and from Phase 2 of the main guide. LLM-use and ethics checks intentionally left out for now. Not yet wired into the Audit Board's data model or statuses — that's a separate decision.
- Made Science Audit mandatory and merged it into Pre-Submission Audit as "Part 2" on the same page (was a separate page) — deleted `science-audit.html`. Pre-Submission Audit is now Part 1 (Format) + Part 2 (Science), both required. Updated the main guide's Phase 2 and the Audit Board's checklist snapshot (now 34 items, each tagged `part: 'format'` or `part: 'science'`) to match.
- Added a new phase, "Submit for Internal Review," between "Write the Paper" and "Submit, Then Keep Working" (8 phases → 9, all renumbered). Moved the "submit first full draft" bullet out of Phase 2 into this new phase, with links to the Audit Board and Pre-Submission Audit.
- Extended the Audit Board past submission to track the paper's full life: three new statuses (Rebuttal, Accepted, Rejected) after Submitted, and four new per-paper fields — submission link (OpenReview or other), rebuttal deadline, rebuttal document link, and free-text review notes. Added an Edit (pencil) button on each card, since these fields get filled in progressively, not at submission time. The "Overdue" badge is now status-aware: it checks the submission deadline pre-submission, the rebuttal deadline during Rebuttal, and nothing once a paper reaches a terminal status (Accepted/Rejected/Withdrawn/Failed).
- Removed "Failed" as a column. Added a safety net alongside it: a card whose status no longer matches any column (e.g. one already marked Failed before this change) now shows in a fallback "Other" column with a note explaining why, instead of silently disappearing from the board.
- Read the actual BOLDiquette (via Google Drive) and added provenance links per feedback: every phase in the main guide now links to the specific BOLDiquette section it's sourced from; Pre-Submission Audit and Audit Board each link to BOLDiquette's ML Conference Cycle section and note that their content (the audit process, the board itself, post-submission tracking) isn't written up there yet. Drafted a proposed BOLDiquette addition (`proposed-boldiquette-addition.md`) covering that gap, matched to the doc's existing voice, for pasting in directly. Flagged two divergences rather than resolving them unasked: the draft-deadline day count (10 vs 21) and the arXiv affiliation line (Oxford-only vs Oxford/Imperial/UCL).
